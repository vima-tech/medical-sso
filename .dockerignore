# 构建上下文是仓库根目录。这里排除的都是体积大又用不上的东西——
# 不排除的话每次构建都要把几百 MB 的本地仓库和 target 打包送进构建器。
.git
.m2
**/target

# 真实密钥绝不能进构建上下文：Containerfile 里是 COPY . .，
# 进了上下文就会落进构建阶段的镜像层。
# 注意必须写成 **/.env——.dockerignore 按完整相对路径匹配，
# 光写 .env 只能排除仓库根目录那一个，排不掉 deploy/.env。
**/.env
**/.env.production
